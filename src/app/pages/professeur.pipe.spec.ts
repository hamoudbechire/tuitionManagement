import { ProfesseurPipe } from './professeur.pipe';

describe('ProfesseurPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfesseurPipe();
    expect(pipe).toBeTruthy();
  });
});
